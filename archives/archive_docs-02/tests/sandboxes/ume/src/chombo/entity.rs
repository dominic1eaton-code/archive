/**
 * @copyright
 * @brief
 * @note
 */

impl EntityManager()
{
    fn create_entity()
    {}

    fn delete_entity()
    {}

    fn view_registry()
    {}

    fn view_capitalization()
    {}

    fn generate_report()
    {}
}

struct EntityData
{
    id: u32,
    name: String,
    role: String,
    token: String,
    dba: String, // doing business as name
};

impl Entity
{
    fn create()
    {}

    // legal entity document registry
    fn create_registry()
    {}

    // create equity,ownership table
    fn create_cap_table()
    {}

    // add,track,manage entity IP
    fn add_IP()
    {}

    // link pamoja organization,team,employees to chombo entity
    fn add_organization()
    {}

    // entity registered agent
    fn add_agent()
    {}

    // e.g. send shareholder meeting reminder notification, integrated with chat platform
    fn send_notification()
    {}

    // create,track meeting minutes and integrate with digital workspace platform
    fn create_minutes()
    {}

    // add sub-entity, acquisition/acquired entity
    fn add_entity()
    {}

    // merge two entites, entity merger
    fn merge_entity()
    {}
}

// track,manage entity IP
impl IP
{
    // add business license (to entity registry)
    fn add_business_license()
    {}

    // show entity business licenses in tile view
    fn show_license()
    {}
}