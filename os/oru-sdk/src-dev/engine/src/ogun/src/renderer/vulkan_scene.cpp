/**
 * @license
 * @brief core vulkan objects
 */

#include "vulkan_scene.h"
#include <iostream>

void ogun::load_scene(uint32_t sceneID)
{
    if (sceneID == 0)
    {
        std::cout << "loading default scene..." << std::endl;
    }
    else
    {
    }
}
